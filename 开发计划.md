# 知乎文章转视频 MVP 开发计划

> 依据《开发文档.md》制定。本计划按一名全栈开发者顺序执行设计；可将“前端工作台”和“内容处理链路”并行，但每一阶段都必须能独立验收。

## 1. 总体排期与交付目标

| 阶段 | 目标                         |  预计工作量 | 阶段交付物                                      |
| ---- | ---------------------------- | ----------: | ----------------------------------------------- |
| P0   | 项目骨架、桌面壳与可验证样例 | 1～1.5 人日 | 可运行 Electron 工程、测试样例、环境检查        |
| P1   | 数据与任务底座               | 1.5～2 人日 | SQLite、任务状态机、文件目录                    |
| P2   | Excel 导入与批次管理         | 1～1.5 人日 | Excel 导入、字段校验、批次/任务 API             |
| P3   | 知乎内容读取                 |   2～3 人日 | 回答/专栏解析器、失败分类与原始快照             |
| P4   | AI 摘要与质量门槛            | 1.5～2 人日 | 结构化摘要、标题/标签、人工确认机制             |
| P5   | 3:4 图片与视频生成           |   2～3 人日 | 卡片 PNG、MP4、尾注、预览产物                   |
| P6   | 工作台与任务记录页面         |   2～3 人日 | 导入、任务表、预览、编辑、下载页面              |
| P7   | 批量交付、跨平台测试与打包   |   2～3 人日 | ZIP、结果 Excel、Windows/macOS 安装包、部署说明 |

**首版总量：约 12～17 人日。**

排期中最大不确定项是知乎页面可访问性与内容结构变化；P3 必须先用测试 Excel 中的真实链接验证通过，再进入完整联调。

## 2. 开发顺序与依赖

```text
P0 项目骨架
  └─ P1 数据与任务底座
       ├─ P2 Excel 导入 ────────────────┐
       ├─ P3 知乎内容读取 ─ P4 AI 摘要 ─┼─ P5 图片/视频生成 ─ P7 交付与测试
       └─ P6 工作台页面 ────────────────┘
```

- P2、P3、P6 可以在 P1 完成后并行。
- P4 依赖 P3 给出标准化正文。
- P5 依赖 P4 给出合格的卡片 JSON。
- P7 依赖 P2～P6 全部完成。

## 3. 阶段计划

### P0：项目骨架与样例基线

**目标**：建立可运行、可测试、可复现的单机工程，不先开发页面功能。

**任务**：

1. 初始化 pnpm 工作区，分为 `apps/web`（React）、`apps/api`（Fastify）与 `apps/desktop`（Electron Main/Preload）。
2. 创建 `data/`、`outputs/`、`logs/`、`fixtures/` 目录，并加入 `.gitignore`。
3. 配置 TypeScript strict、ESLint、Prettier、Vitest 与基础 Playwright Test。
4. 编写环境检查命令：Node、Electron、Chromium、FFmpeg、写入目录、AI API 配置是否可用。
5. 建立桌面安全基线：`contextIsolation: true`、`nodeIntegration: false`；Preload 仅暴露白名单 IPC 方法。
6. 约定跨平台数据目录：测试模式使用临时目录，正式版使用 Electron `userData`；禁止依赖 macOS 专用绝对路径。
7. 将测试 Excel 复制为受控测试夹具；补充 2～3 条人工构造的非法链接、空口令、空标题样例。

**验收**：

- 前端和 API 均可本地启动。
- `lint`、`typecheck`、`test` 均能运行。
- 环境检查能指出缺失的 FFmpeg、Chromium 或 AI 配置，不泄露密钥。
- macOS 和 Windows 的桌面壳都能启动同一套 React 页面；Preload 不直接暴露 `ipcRenderer`、`fs` 或 Node 全局对象。

### P1：数据模型与任务执行底座

**目标**：让任务在浏览器关闭、进程重启后仍能知道自己已完成到哪一步。

**任务**：

1. 编写 SQL 迁移：`batches`、`article_tasks`、`task_attempts` 三张表。
2. 实现任务状态机：`pending → fetching → summarizing → rendering_images → rendering_video → completed`，并支持 `failed` 与 `needs_review`。
3. 实现单进程 Worker：领取一个可执行任务、写入步骤日志、保存进度、捕获错误。
4. 实现“从失败步骤重试”，禁止重复调用已成功的上游步骤。
5. 规定所有任务文件写入 `outputs/{batchId}/{taskId}/`。

**测试优先**：先测试状态迁移非法时会被拒绝、失败后可从正确步骤恢复、同一任务不会被重复领取。

**验收**：

- 重启 API 后，任务状态与步骤日志仍完整。
- 一个失败任务重试时只执行必要步骤。
- 不引入 Redis、BullMQ 或额外队列服务。

### P2：Excel 导入与批次管理

**目标**：把用户的链接表稳定转成可处理任务。

**任务**：

1. 实现 `POST /api/batches/import`，接收 `.xlsx` 文件。
2. 读取第一个非空 Sheet，并按表头识别“日期、知乎标题、链接、文章口令”。
3. 标准化 URL，识别 `answer` 与 `article` 两种来源类型。
4. 按行校验：空链接、重复链接、非法域名、格式不支持、口令超长等。
5. 一次导入创建一个批次；合法行创建任务，非法行写入导入错误清单，但不阻塞合法行。
6. 实现批次列表与批次详情查询 API。

**测试优先**：针对当前测试 Excel、缺少“链接”列、无效链接、重复链接、空口令分别写解析测试。

**验收**：

- 测试 Excel 的有效行全部被导入，且回答/专栏类型正确。
- 前端能准确展示导入行数、成功数和错误行号。
- 导入后的口令不被 AI 覆盖。

### P3：知乎内容读取与失败分型

**目标**：将可访问链接转换为可供 AI 使用的、可追溯的正文。

**任务**：

1. 用 Playwright 实现独立的 `AnswerExtractor` 与 `ArticleExtractor`。
2. 提取标题、作者（若公开）、正文段落、链接和读取时间；不处理评论区。
3. 清理导航、推荐、广告、页脚和重复文本，保留段落序号。
4. 将原始 HTML 与清洗后的结构化正文存到任务目录。
5. 建立错误分类：`SOURCE_NOT_FOUND`、`SOURCE_ACCESS_RESTRICTED`、`CONTENT_EMPTY`、`SOURCE_LAYOUT_CHANGED`、`NETWORK_ERROR`。
6. 对真实链接建立小规模回归夹具：保存允许保存的解析结果摘要，不保存账号凭证。

**测试优先**：为两种来源分别提供 HTML 夹具，锁定标题、正文去噪与错误分型；真实链接测试只做受控冒烟。

**验收**：

- 对测试 Excel 中至少各一条“回答”和“专栏”能成功读取并产出段落化正文。
- 无法读取的页面进入正确失败/人工确认状态，不能假装成功。
- 不读取、不持久化用户 Cookie 或密码。

### P4：AI 摘要、标题、标签与质检

**目标**：把正文变成可直接渲染的、短而可读的卡片数据。

**任务**：

1. 定义并实现 JSON Schema：`videoTitle`、`tags`、`pages`、`sourceRefs`、`riskFlags`。
2. 实现模型适配器；模型名称、接口地址和密钥仅从环境变量读取。
3. 编写摘要提示词：保留原文事实、一个卡片一个观点、避免虚构、避免照抄长段落。
4. 后端校验页数、字数、标签数、来源段落引用和 JSON 格式。
5. 校验失败、内容过短或口令为空时转为 `needs_review`。
6. 保存请求摘要、模型响应和校验结果，支持人工修改后跳过或重跑 AI 步骤。

**测试优先**：测试合法 JSON、缺字段 JSON、超长卡片、无来源引用、过少/过多页面与空口令的处理。

**验收**：

- 模型输出不合格时不会进入渲染步骤。
- 每张正文卡片能回溯到至少一个正文段落。
- 人工确认后的标题、标签、口令和尾注可被后续渲染使用。

### P5：固定卡片渲染与视频合成

**目标**：把合格的卡片 JSON 变成可预览、可下载的 3:4 图片和视频。

**任务**：

1. 实现唯一的固定卡片视觉模板：封面、正文、尾页三种版式。
2. 固定画布比例 3:4，建议输出 `1080×1440` PNG；控制字体、断行、页码和安全边距。
3. 使用 Playwright 将 HTML 卡片截图为顺序 PNG。
4. 用 FFmpeg 合成 H.264/AAC MP4：封面 1 秒、正文每页 1 秒、尾页 2 秒。
5. 生成 `metadata.json`，记录图片顺序、时长、最终标题、标签、尾注与源链接。
6. 在任务目录保留 FFmpeg 命令和错误日志，便于失败重试。

**测试优先**：用固定 AI JSON 测试卡片断行、3:4 尺寸、图片顺序、视频时长和尾注文案替换。

**验收**：

- 成功任务包含封面、正文、尾页 PNG 与 MP4。
- 正文每页为 1 秒，尾页为 2 秒，视频总时长符合公式。
- 预览画面无文字截断、空白页或错误口令。

### P6：工作台与任务记录页面

**目标**：按效果图完成单操作者可用页面，并只呈现已批准的功能。

**任务**：

1. 实现侧栏：仅“工作台”“任务记录”两个入口。
2. 实现工作台顶栏：产品名称与“导入 Excel”主按钮；移除头像、通知、帮助、设置、套餐/算力区。
3. 实现三个统计卡：待处理、已生成、需人工确认。
4. 实现任务表：标题、来源、状态、进度、更新时间、操作；支持筛选与刷新。
5. 实现右侧 3:4 视频预览、图片/视频下载和尾注编辑。
6. 实现“需人工确认”编辑面板：标题、标签、口令、尾注；保存后可重试。
7. 实现任务记录页：批次列表、批次详情、状态筛选、批量重试和批量下载。
8. 将文件选择、导出目录选择和成品打开操作接到桌面 Preload 白名单 API；浏览器开发模式保留对应 Mock。

**测试优先**：为导入按钮、状态筛选、失败重试、尾注保存、下载按钮写组件/端到端测试；对效果图建立关键页面截图基线。

**验收**：

- 页面中不出现用户管理、设置、模板中心、素材库、算力、套餐、通知或账号入口。
- 已生成任务能在右侧立即预览和下载。
- 编辑尾注仅影响对应任务，并在重新渲染后反映到尾页。
- 页面脚本不能直接调用 Node、文件系统或任意 IPC Channel。

### P7：批量交付、质量保证与部署

**目标**：以真实测试 Excel 完整跑通一次，形成可交付的本机软件。

**任务**：

1. 实现图片 ZIP、视频下载、批次 ZIP 和 `result.xlsx` 导出。
2. 汇总并展示批次级成功、失败、人工确认数量和失败原因。
3. 补充 API、任务状态机、解析器、AI 校验器、视频时长的自动化测试。
4. 使用 Playwright 做核心路径端到端测试：导入 → 启动 → 预览 → 编辑尾注 → 重试 → 下载。
5. 执行真实测试 Excel 冒烟：至少验证一条回答和一条专栏的成品；访问受限项应被明确记录。
6. 完成 README：本机依赖、AI 环境变量、运行命令、数据目录、常见故障与恢复方式。
7. 执行 lint、typecheck、测试、依赖漏洞扫描和人工代码审查。
8. 通过 Electron 官方预构建二进制和自定义打包脚本，在 macOS 与 Windows Runner 分别打包；在干净系统中启动、导入一个测试 Excel 并下载成品。
9. 准备正式签名配置：macOS Developer ID + Notarization、Windows 代码签名证书。没有证书时仅发布内部测试包，不能宣称“正式可分发”。

**验收**：

- 一批任务中单条失败不会阻塞其余任务。
- ZIP 与 `result.xlsx` 中的任务状态、文件名和实际输出一致。
- 从零环境按 README 能启动、导入、处理和下载。
- macOS 与 Windows 的安装包均能启动；数据文件写入用户数据目录，路径分隔符不影响输出和下载。

## 4. 最小接口/页面交付清单

| 交付项               | P2  | P3  | P4  | P5  | P6  | P7  |
| -------------------- | :-: | :-: | :-: | :-: | :-: | :-: |
| Excel 导入与字段错误 |  ✓  |     |     |     |  ✓  |  ✓  |
| 批次与任务状态       |  ✓  |  ✓  |  ✓  |  ✓  |  ✓  |  ✓  |
| 知乎回答/专栏读取    |     |  ✓  |     |     |     |  ✓  |
| AI 卡片/标题/标签    |     |     |  ✓  |     |  ✓  |  ✓  |
| 3:4 图片与 MP4       |     |     |     |  ✓  |  ✓  |  ✓  |
| 尾注编辑与重渲染     |     |     |  ✓  |  ✓  |  ✓  |  ✓  |
| 单条下载             |     |     |     |  ✓  |  ✓  |  ✓  |
| 批量 ZIP/结果 Excel  |     |     |     |     |  ✓  |  ✓  |

## 5. 关键风险与前置决定

| 风险/决定                    | 影响           | 计划中的处理                                                                    |
| ---------------------------- | -------------- | ------------------------------------------------------------------------------- |
| 知乎页面结构、登录或访问限制 | P3 成功率      | 先用真实链接做冒烟；失败分类而非绕过访问控制                                    |
| 模型与密钥尚未确定           | P4 无法真连调  | 先按 JSON Schema 完成适配接口和 Mock；接入时由用户提供合法可用配置              |
| 1 秒阅读时间很短             | 卡片可读性     | 限制正文卡片字数、封面/正文每页仅一个观点、尾页固定 2 秒                        |
| 长文内容过多                 | 视频过长       | 默认 6～15 页；超过上限进入人工确认或后续拆集                                   |
| FFmpeg/Chromium 机器依赖     | P5/P7 部署失败 | P0 环境检查；Docker 仅作为内部服务器可选方案                                    |
| macOS/Windows 原生打包差异   | P0/P7 交付失败 | 使用 Electron 官方预构建二进制，在两种目标系统的 CI Runner 原生打包并做安装冒烟 |
| 未签名应用被系统拦截         | 正式分发受阻   | 预留签名/公证配置；正式发布前必须准备 Apple 与 Windows 证书                     |
| 外部内容版权与使用授权       | 合规风险       | 只处理用户有权使用的内容；保留原文链接与来源追溯                                |

## 6. 发布门槛

满足以下条件才可交付 MVP：

1. 全量自动化检查通过：格式、lint、TypeScript、单元测试、端到端测试。
2. 基于测试 Excel 的真实链路冒烟已执行；成功和失败都有明确证据。
3. 成功任务的 PNG、MP4、尾页、标题、标签和 `metadata.json` 一致。
4. 页面没有任何超出范围的用户、设置、算力、模板、套餐或发布平台入口。
5. 失败任务可解释、可重试；没有静默失败或伪成功。
6. README 已写明运行环境、密钥配置方式、数据位置、恢复与清理方式。
7. macOS 与 Windows 的安装包都完成原生系统冒烟；正式分发包已签名，或明确标为未签名内部测试包。
