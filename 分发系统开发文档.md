# 视频分发系统开发文档

## 1. 产品定位

基于现有"知乎文章转视频"桌面工具的视频分发平台。本机批量生成视频后自动上传至阿里云 OSS，管理员通过网页管理端创建任务池、生成活码二维码，用户扫码填写信息后领取指定数量的视频任务并下载。

核心价值：将"本机生产 → 云端存储 → 扫码分发 → 用户下载"闭环自动化，支撑 50+ 人同时扫码领取的并发场景。

## 2. 系统边界

### 2.1 本期包含

- 视频生成完成后自动上传阿里云 OSS，生成唯一文件 key 与预签名下载链接。
- 管理端（网页）：任务池管理、活码生成与管理、领取记录查看、用户管理。
- 用户端（H5）：扫码打开、填写信息、选择领取数量、查看已领取视频、下载。
- 活码机制：短码路由，同一二维码可动态切换绑定任务池，支持过期与统计。
- 并发领取控制：库存扣减、防重复领取、任务唯一分配。
- 服务端登记：每次领取记录用户信息、领取时间、分配的视频任务 ID。

### 2.2 明确不做

- 视频在线编辑、转码、水印。
- 用户付费、套餐、算力、积分体系。
- 视频在线播放（仅提供下载）。
- 多管理员协作、角色权限细分。
- 自动推送到微信/短信通知。

## 3. 用户角色与使用流程

### 3.1 管理员（运营者）

```text
本机生成视频 → 自动上传 OSS
       ↓
登录管理端网页
       ↓
创建任务池（关联已上传的视频）
       ↓
生成活码（QR 图片）→ 发到用户群
       ↓
查看领取统计、剩余库存
```

### 3.2 终端用户（领取者）

```text
扫码 → 打开 H5 领取页
       ↓
填写信息（姓名 + 手机号）
       ↓
选择领取数量（受库存与单人上限约束）
       ↓
确认领取 → 服务端分配具体视频任务
       ↓
查看已领取列表 → 点击下载（预签名 URL）
```

## 4. 系统架构

### 4.1 整体架构

```text
┌─────────────────────────────────────────────────────────────┐
│                        阿里云 OSS                            │
│              存储视频文件（MP4）+ 图片资源                     │
└──────────────────────────┬──────────────────────────────────┘
                           │ 上传 / 预签名下载
┌──────────────────────────┼──────────────────────────────────┐
│                    分发系统服务端                              │
│  ┌───────────┐  ┌──────────────┐  ┌───────────────────┐     │
│  │ 上传模块  │  │ 活码/任务池  │  │ 用户/领取/下载    │     │
│  └───────────┘  └──────────────┘  └───────────────────┘     │
│                    MySQL / PostgreSQL                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
          ┌────────────────┼────────────────┐
          │                                 │
┌─────────▼─────────┐           ┌──────────▼──────────┐
│   管理端（Web）    │           │   用户端（H5）       │
│  任务池/活码/统计  │           │  扫码/填写/领取/下载 │
└───────────────────┘           └─────────────────────┘
          ▲
          │ 上传完成回调
┌─────────┴─────────┐
│  本机桌面工具      │
│  视频生成 + 上传   │
└───────────────────┘
```

### 4.2 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 分发后端 | Node.js + Fastify + TypeScript | 与现有项目技术栈一致 |
| 数据库 | MySQL 8.0（或 PostgreSQL） | 多用户并发、事务支持 |
| ORM/查询 | Drizzle ORM | 类型安全、轻量、支持迁移 |
| 云存储 | 阿里云 OSS（ali-oss SDK） | 视频存储 + 预签名 URL |
| 管理端前端 | React 19 + Vite + Ant Design | 后台管理界面 |
| 用户端 H5 | React 19 + Vite（移动端适配） | 扫码领取页面 |
| 用户认证（管理端） | 账号密码 + JWT | 管理员登录 |
| 用户认证（用户端） | 手机号 + 验证码 / 微信 OAuth | 终端用户身份识别 |
| QR 码生成 | qrcode（前端 canvas 渲染） | 管理端生成活码图片 |
| 部署 | 阿里云 ECS / 轻量服务器 + Nginx | 单实例即可承载 |

### 4.3 与现有桌面工具的集成

在现有 `task-worker.ts` 任务完成后新增上传步骤：

```text
rendering_video → completed → uploading → uploaded
```

- 上传目标：阿里云 OSS bucket，key 格式 `videos/{batchId}/{taskId}/video.mp4`
- 上传成功后回调分发系统 API，注册视频资源（文件 key、大小、时长、关联元数据）
- 上传失败不影响本地任务状态，支持手动/自动重试

## 5. 数据模型

### 5.1 视频资源表 `video_assets`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| file_key | VARCHAR(512) UNIQUE | OSS 文件 key |
| file_name | VARCHAR(255) | 原始文件名 |
| file_size | BIGINT | 文件大小（字节） |
| duration_seconds | INT | 视频时长（秒） |
| title | VARCHAR(255) | 视频标题 |
| source_url | VARCHAR(1024) | 知乎原文链接 |
| batch_id | VARCHAR(64) | 来源批次 ID |
| task_id | VARCHAR(64) | 来源任务 ID |
| status | ENUM | `available` / `assigned` / `disabled` |
| created_at | DATETIME | 上传时间 |

### 5.2 任务池表 `task_pools`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| name | VARCHAR(128) | 任务池名称 |
| description | TEXT | 描述（可选） |
| total_count | INT | 总视频数 |
| remaining_count | INT | 剩余可领取数 |
| max_per_user | INT | 单人最大领取数 |
| status | ENUM | `active` / `paused` / `expired` |
| expire_at | DATETIME NULL | 过期时间（NULL 为永不过期） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.3 任务池-视频关联表 `pool_videos`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| pool_id | BIGINT FK | 所属任务池 |
| video_asset_id | BIGINT FK | 关联视频资源 |
| UNIQUE(pool_id, video_asset_id) | | 防重复关联 |

### 5.4 活码表 `qr_codes`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| short_code | VARCHAR(16) UNIQUE | 短码（如 `A3xK9`） |
| pool_id | BIGINT FK | 当前绑定的任务池 |
| name | VARCHAR(128) | 活码备注名 |
| scan_count | INT DEFAULT 0 | 累计扫码次数 |
| status | ENUM | `active` / `disabled` |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 最后修改时间 |

### 5.5 用户表 `users`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| phone | VARCHAR(20) UNIQUE NULL | 手机号 |
| name | VARCHAR(64) | 姓名 |
| wechat_openid | VARCHAR(128) UNIQUE NULL | 微信 openid |
| created_at | DATETIME | 首次注册时间 |
| last_active_at | DATETIME | 最后活跃时间 |

### 5.6 领取记录表 `claim_records`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| user_id | BIGINT FK | 领取用户 |
| pool_id | BIGINT FK | 来源任务池 |
| qr_code_id | BIGINT FK NULL | 来源活码 |
| video_asset_id | BIGINT FK | 分配的视频资源 |
| claimed_at | DATETIME | 领取时间 |
| downloaded_at | DATETIME NULL | 首次下载时间 |
| UNIQUE(user_id, video_asset_id) | | 防重复分配 |

### 5.7 管理员表 `admins`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 自增主键 |
| username | VARCHAR(64) UNIQUE | 登录账号 |
| password_hash | VARCHAR(255) | bcrypt 哈希 |
| created_at | DATETIME | 创建时间 |

## 6. API 设计

### 6.1 管理端 API（需 JWT 鉴权）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/admin/auth/login` | 管理员登录，返回 JWT |
| POST | `/admin/assets/register` | 注册已上传的视频资源（桌面端回调） |
| GET | `/admin/assets` | 视频资源列表（分页、筛选） |
| POST | `/admin/pools` | 创建任务池 |
| GET | `/admin/pools` | 任务池列表 |
| GET | `/admin/pools/:id` | 任务池详情（含关联视频） |
| PUT | `/admin/pools/:id` | 编辑任务池（名称、上限、过期时间） |
| POST | `/admin/pools/:id/videos` | 向任务池添加视频 |
| DELETE | `/admin/pools/:id/videos/:videoId` | 从任务池移除视频 |
| PATCH | `/admin/pools/:id/status` | 暂停/恢复/过期任务池 |
| POST | `/admin/qr-codes` | 创建活码（绑定任务池） |
| GET | `/admin/qr-codes` | 活码列表 |
| PUT | `/admin/qr-codes/:id` | 修改活码（切换绑定池、备注） |
| PATCH | `/admin/qr-codes/:id/status` | 启用/停用活码 |
| GET | `/admin/claims` | 领取记录列表（按池/用户/时间筛选） |
| GET | `/admin/stats/overview` | 总览统计（视频数、领取数、用户数） |

### 6.2 用户端 API（H5）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/s/:shortCode` | 活码入口：验证有效性，返回任务池信息 + 领取页配置 |
| POST | `/claim/send-code` | 发送手机验证码 |
| POST | `/claim/verify` | 验证手机号 + 验证码，返回用户 token |
| GET | `/claim/pool/:poolId/info` | 获取任务池公开信息（剩余数量、单人上限） |
| POST | `/claim/submit` | 提交领取请求（姓名、手机号/token、数量） |
| GET | `/claim/my-videos` | 我的已领取视频列表 |
| GET | `/claim/download/:videoAssetId` | 获取预签名下载 URL（有效期 1 小时） |

### 6.3 桌面端上传 API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/upload/presign` | 获取 OSS 直传预签名 URL（桌面端直传 OSS） |
| POST | `/upload/complete` | 上传完成确认，注册视频资源 |

## 7. 活码机制详细设计

### 7.1 原理

```text
QR 码内容 = https://{domain}/s/{shortCode}
```

- QR 码图片生成后内容固定，无需重新生成
- 服务端通过 `short_code` 查找当前绑定的任务池
- 管理员可随时切换绑定、停用、设置过期

### 7.2 短码生成规则

- 长度：6 位，字符集 `ABCDEFGHJKLMNPQRSTUVWXYZ23456789`（去除易混淆的 0/O/1/I）
- 生成方式：服务端随机生成 + 唯一性校验
- 示例：`A3xK9P`、`7TN2QW`

### 7.3 扫码流程

```text
用户扫码 → GET /s/{shortCode}
  → 查 qr_codes 表：是否存在、是否 active
  → 查关联 task_pools：是否 active、是否过期、remaining > 0
  → 有效：返回 302 重定向到 H5 领取页 /claim/{poolId}
  → 无效：返回"活动已结束"提示页
  → 同时 scan_count + 1（异步，不阻塞响应）
```

### 7.4 管理端 QR 码生成

- 前端使用 `qrcode` npm 包在 canvas 上渲染
- 支持下载 PNG（300×300 / 600×600）
- 支持复制图片到剪贴板
- 活码 URL 固定，切换任务池无需重新发码

## 8. 并发领取控制

### 8.1 库存扣减（原子操作）

```sql
UPDATE task_pools
SET remaining_count = remaining_count - :quantity
WHERE id = :poolId
  AND remaining_count >= :quantity
  AND status = 'active'
  AND (expire_at IS NULL OR expire_at > NOW());
```

影响行数为 0 则库存不足，拒绝领取。

### 8.2 任务分配（防重复）

```sql
-- 在事务中锁定未分配的视频
SELECT pv.video_asset_id
FROM pool_videos pv
JOIN video_assets va ON va.id = pv.video_asset_id
WHERE pv.pool_id = :poolId
  AND va.status = 'available'
LIMIT :quantity
FOR UPDATE SKIP LOCKED;

-- 标记已分配
UPDATE video_assets SET status = 'assigned' WHERE id IN (...);

-- 写入领取记录
INSERT INTO claim_records (user_id, pool_id, qr_code_id, video_asset_id, claimed_at)
VALUES ...;
```

### 8.3 防重复领取

- `claim_records` 表 `UNIQUE(user_id, video_asset_id)` 约束
- 同一用户在同一任务池的累计领取数不超过 `max_per_user`

### 8.4 并发能力评估

50 人同时扫码领取：
- 数据库事务 + 行锁完全足够
- 无需 Redis、消息队列或分布式锁
- 单 MySQL 实例可轻松支撑数百 QPS

## 9. 阿里云 OSS 集成

### 9.1 上传方案：客户端直传

```text
桌面端 → POST /upload/presign → 服务端返回预签名 PUT URL
桌面端 → PUT 视频文件到 OSS（直传，不经服务端）
桌面端 → POST /upload/complete → 服务端确认并注册资源
```

优势：视频文件不经过分发服务器，节省带宽。

### 9.2 下载方案：预签名 URL

- 用户点击下载时，服务端生成 1 小时有效的预签名 GET URL
- 用户浏览器直接从 OSS 下载
- URL 过期后需重新获取

### 9.3 OSS 配置

| 配置项 | 说明 |
|--------|------|
| Bucket | 私有读写，不允许公开访问 |
| Region | 按服务器所在区域选择 |
| 文件 Key 格式 | `videos/{batchId}/{taskId}/video.mp4` |
| 预签名有效期 | 上传 30 分钟，下载 1 小时 |
| 生命周期规则 | 可选：90 天未下载自动转低频存储 |

## 10. 用户认证

### 10.1 管理端

- 账号密码登录 → 签发 JWT（有效期 24 小时）
- 所有 `/admin/*` 接口需携带 `Authorization: Bearer {token}`
- 首版单管理员，后续可扩展多角色

### 10.2 用户端（H5）

**方案 A：手机号 + 验证码（推荐首版）**

```text
填写手机号 → 发送验证码（阿里云 SMS）→ 输入验证码 → 验证通过
→ 查找/创建用户 → 签发临时 token（有效期 7 天）
→ 后续请求携带 token 识别身份
```

**方案 B：微信 OAuth（微信内打开时）**

```text
微信内打开 H5 → 静默授权（scope=snsapi_base）→ 获取 openid
→ 查找/创建用户 → 自动登录
```

首版优先实现方案 A（无微信开放平台依赖），方案 B 作为增强。

## 11. 页面设计

### 11.1 管理端页面

```text
左侧导航
├── 总览（统计面板）
├── 视频资源
├── 任务池管理
├── 活码管理
├── 领取记录
└── 系统设置
```

**总览页**：视频总数、已分配数、剩余数、今日领取数、用户总数。

**视频资源页**：表格展示所有已上传视频，支持搜索、筛选状态、手动添加到任务池。

**任务池管理页**：
- 创建/编辑任务池（名称、描述、单人上限、过期时间）
- 向池中添加入库视频（从视频资源中选择）
- 查看池内视频列表、剩余库存
- 暂停/恢复/关闭任务池

**活码管理页**：
- 创建活码（选择绑定任务池、备注名）
- 显示 QR 码图片，支持下载/复制
- 切换绑定任务池（活码不变）
- 启用/停用
- 扫码次数统计

**领取记录页**：表格展示所有领取记录（用户、视频、任务池、时间），支持导出 Excel。

### 11.2 用户端 H5 页面

**领取页**（`/claim/:poolId`）：
- 顶部：任务池名称 + 剩余数量
- 表单：姓名（必填）、手机号（必填）、验证码
- 领取数量选择（受 max_per_user 和 remaining 限制）
- 提交按钮

**我的视频页**（领取成功后跳转）：
- 已领取视频列表（标题、领取时间）
- 每条显示"下载"按钮
- 下载调用预签名 URL

## 12. 桌面端上传集成

### 12.1 新增配置

在 `.env` 中增加：

```env
DISTRIBUTE_API_URL=https://your-domain.com
DISTRIBUTE_API_KEY=xxxxx
OSS_REGION=oss-cn-hangzhou
OSS_BUCKET=your-bucket
OSS_ACCESS_KEY_ID=xxxxx
OSS_ACCESS_KEY_SECRET=xxxxx
```

### 12.2 上传时机

在现有任务状态机中，`completed` 后新增可选步骤：

```text
completed → uploading → uploaded
           ↘ upload_failed（可重试）
```

- 配置了 `DISTRIBUTE_API_URL` 时自动上传
- 未配置时跳过，不影响本地功能
- 上传失败记录错误，支持手动重试

### 12.3 上传流程

1. 请求预签名 URL：`POST {DISTRIBUTE_API_URL}/upload/presign`
2. 直传 OSS：`PUT {presignedUrl}` + 文件流
3. 确认完成：`POST {DISTRIBUTE_API_URL}/upload/complete`（携带 file_key、元数据）

## 13. 安全设计

| 层面 | 措施 |
|------|------|
| OSS 访问 | Bucket 私有，所有访问通过预签名 URL，有效期限制 |
| 管理端 | JWT 鉴权，密码 bcrypt 存储，接口限流 |
| 用户端 | 验证码防刷（60 秒间隔、每日上限），领取接口限流 |
| 活码 | 短码不可枚举（6 位 32 字符集 = 10 亿组合） |
| 下载链接 | 预签名 URL 1 小时过期，不可伪造 |
| 数据传输 | 全站 HTTPS |

## 14. 部署架构

```text
┌─────────────────────────────────────────┐
│           阿里云 ECS / 轻量服务器         │
│                                         │
│  Nginx（反向代理 + 静态资源 + HTTPS）     │
│    ├── /admin/*  → 管理端 API (3001)    │
│    ├── /s/*      → 活码路由 (3001)      │
│    ├── /claim/*  → 用户端 API (3001)    │
│    ├── /admin-ui → 管理端前端静态文件    │
│    └── /h5       → 用户端 H5 静态文件   │
│                                         │
│  Node.js 服务 (Fastify, port 3001)      │
│  MySQL 8.0 (port 3306)                  │
└─────────────────────────────────────────┘
         ↕
┌─────────────────┐
│   阿里云 OSS    │
│  视频文件存储    │
└─────────────────┘
```

单台 2C4G 服务器即可支撑 50 人并发场景。

## 15. 开发阶段

### D1：基础骨架与数据库（1 人日）

- 初始化分发系统项目（可独立仓库或 monorepo 子包）
- 数据库建表与迁移
- Fastify 服务骨架、健康检查
- 管理员账号初始化与 JWT 鉴权中间件

### D2：OSS 集成与视频上传（1 人日）

- 阿里云 OSS SDK 集成
- 预签名上传/下载 URL 生成
- 视频资源注册 API
- 桌面端上传模块（集成到现有 task-worker）

### D3：任务池与活码管理（1.5 人日）

- 任务池 CRUD API
- 视频关联/解绑
- 活码 CRUD + 短码生成
- 活码路由（`/s/:shortCode` 重定向）

### D4：用户端领取流程（1.5 人日）

- 手机验证码发送与校验
- 用户注册/查找
- 领取接口（并发控制、库存扣减、任务分配）
- 我的视频列表 + 预签名下载

### D5：管理端前端（2 人日）

- 登录页
- 总览统计面板
- 视频资源管理页
- 任务池管理页（含添加视频）
- 活码管理页（含 QR 码生成/下载）
- 领取记录页

### D6：用户端 H5 前端（1 人日）

- 领取页（表单 + 数量选择）
- 我的视频页（列表 + 下载）
- 移动端适配
- 活动结束/库存不足提示页

### D7：联调、测试与部署（1 人日）

- 端到端流程验证（上传 → 建池 → 生码 → 扫码 → 领取 → 下载）
- 并发领取压测（模拟 50 人同时领取）
- 部署到服务器、配置 Nginx + HTTPS
- 桌面端上传联调

**总计：约 9 人日。**

## 16. 验收标准

1. 桌面端生成视频后自动上传 OSS，管理端可见视频资源。
2. 管理员可创建任务池、添加视频、生成活码并下载 QR 图片。
3. 用户扫码后打开 H5 页面，填写信息并选择数量后成功领取。
4. 50 人同时扫码领取不超发、不重复分配。
5. 同一用户不超过单人上限。
6. 领取后用户可下载视频（预签名 URL 有效）。
7. 活码可切换绑定任务池，无需重新生成二维码。
8. 过期/停用的活码扫码后显示"活动已结束"。
9. 管理端可查看完整领取记录和统计数据。
10. 全站 HTTPS，OSS 不公开访问。
