# 知乎文章转视频开发文档（MVP）

## 1. 产品定位

这是一个单机、单操作者使用的批量内容生产工具：导入包含知乎链接的 Excel 后，系统读取可访问的文章内容，通过 AI 生成适合 3:4 竖版阅读的摘要卡片，将卡片导出为图片并合成为视频，同时生成可发布的标题、标签和知乎原文引导尾页。

首版目标是稳定完成“链接表 → 可审核的图文视频素材”这一闭环，而非建设内容平台。

## 2. 范围

### 2.1 本期包含

- Excel 批量导入、字段校验和任务创建。
- 支持知乎回答链接与知乎专栏链接。
- 获取可正常访问的标题、正文等内容；无法获取时给出明确失败原因。
- AI 输出结构化摘要卡片、发布标题和标签。
- 以固定视觉样式生成 3:4 图片卡片。
- 将图片卡片合成为 MP4；正文页停留 1 秒。
- 生成、编辑并应用最后一页尾注。
- 单条预览、下载视频、下载图片、批量下载。
- 任务进度、失败重试和人工确认。

### 2.2 明确不做

- 用户注册、登录、权限、团队、角色、套餐与用户管理。
- 设置中心、通知中心、帮助中心、用量统计、算力/额度显示。
- 模板中心和多模板编辑器；首版只提供一套固定视觉模板。
- 自动发布到抖音、小红书、视频号等平台。
- AI 配音、数字人、AI 视频生成、复杂转场和时间轴编辑。
- 绕过知乎登录、付费、访问限制或风控。

## 3. 目标用户与使用方式

目标用户是负责运营知乎技术内容的单个内容编辑。产品以 Windows 与 macOS 桌面应用交付，默认无登录页、无远程多租户服务；任务数据和成品默认保存在当前操作系统的应用数据目录，并允许用户选择导出目录。

标准路径：

1. 编辑准备 Excel，上传到工作台。
2. 系统校验每行链接并创建文章任务。
3. 系统依次读取内容、生成摘要、渲染图片、合成视频。
4. 编辑对“需人工确认”的任务修改口令、标题、标签或尾注后重试。
5. 编辑预览并下载单条成品或整批 ZIP。

## 4. 输入与输出

### 4.1 Excel 输入约定

首版读取第一个非空工作表，识别以下列：

| 列名     |     必填 | 用途               | 校验                                                  |
| -------- | -------: | ------------------ | ----------------------------------------------------- |
| 日期     |       否 | 批次归档与筛选     | 可解析的日期                                          |
| 知乎标题 |       否 | 与获取到的标题比对 | 文本                                                  |
| 链接     |       是 | 内容来源           | 仅接受 `zhihu.com/answer/` 或 `zhuanlan.zhihu.com/p/` |
| 文章口令 | 建议必填 | 尾注中的检索词     | 2～30 个字符，禁止换行                                |

测试文件已经具备上述表头；首版不要求用户改变该格式。

### 4.2 每篇文章的输出

```text
outputs/{batchId}/{taskId}/
  video.mp4                 # 3:4 图文视频
  images/
    01-cover.png
    02-page.png ...         # 正文卡片
    NN-tail.png             # 尾注卡片
  metadata.json             # 可复跑的结构化结果
```

批次额外输出 `result.xlsx`，包含原始字段、最终标题、标签、状态、失败原因、视频路径和图片 ZIP 路径。

## 5. 信息架构与页面设计

页面依据已确认的工作台效果图，但删除无关入口后收敛为两个页面。

```text
左侧导航
├── 工作台
└── 任务记录

工作台
├── 导入 Excel
├── 批次概览
├── 文章任务表
└── 当前任务预览与尾注编辑

任务记录
├── 批次列表
└── 批次详情 / 批量下载
```

### 5.1 工作台

**布局**：桌面端优先，最小宽度 1280px。左侧 224px 固定导航；主内容区采用“任务表 65% + 预览区 35%”两栏布局。

**顶栏**：产品名称“知乎文章转视频”和主操作按钮“导入 Excel”。不显示通知、帮助、头像、账号或任何用量信息。

**概览卡片**：保留三个与当前批次直接相关的数字：

- 待处理
- 已生成
- 需人工确认

不显示“本月使用量”“升级套餐”“算力”等无关内容。

**任务表字段**：勾选框、文章标题、来源、状态、进度、更新时间、操作。

操作按状态显示：

- 待处理：开始处理
- 处理中：查看详情
- 已生成：预览、下载视频、下载图片、重新生成
- 需人工确认：编辑并重试
- 失败：查看原因、重试

**右侧预览区**：

- 固定显示 3:4 成片画布。
- 显示当前页码、播放进度、静音开关和全屏预览。
- 显示“下载视频”“下载图片”按钮。
- 下方显示尾注编辑框；编辑后仅影响当前任务，点击“保存并重新渲染尾页”生效。

### 5.2 任务记录

展示历史导入批次：导入时间、文件名、总数、完成数、失败数、状态和操作。

批次详情可筛选状态、批量重试失败任务、下载已完成任务 ZIP、下载 `result.xlsx`。不单独建设“素材库”；成品通过任务或批次下载即可定位。

## 6. 内容与视频规则

### 6.1 卡片构成

每条任务生成以下卡片：

1. 封面：AI 生成的短视频标题，显示来源“知乎”。
2. 正文：AI 根据文章观点切分为 N 张卡片。
3. 尾页：固定引导文案与文章口令。

每张正文卡片只表达一个观点，包含短标题和极短解释。正文不直接复制整段原文。

### 6.2 时长规则

- 封面：1 秒。
- 正文卡片：每页 1 秒。
- 尾页：默认 2 秒；这是唯一建议长于 1 秒的页面，以保证用户能读清检索口令。
- 视频总长度：`1 + 正文页数 × 1 + 2` 秒。

默认正文卡片数量为 6～15 页。若内容不足 3 个有效观点，标记为“内容过短，需人工确认”；若超过 15 页，优先要求 AI 压缩，仍无法压缩时标记为“建议拆分上下集”。

### 6.3 尾注规则

默认模板：

> 来知乎搜索🔍{文章口令}可以看到全文

`文章口令`优先使用 Excel 中的值。为空时，AI 只能生成“候选口令”，状态必须为“需人工确认”；不可自动声明该候选口令一定能检索到原文。

## 7. 系统架构

### 7.1 前端技术栈

| 分类       | 技术                        | 用途                                                                     |
| ---------- | --------------------------- | ------------------------------------------------------------------------ |
| 语言与框架 | TypeScript、React、Vite     | 开发桌面优先的工作台页面与本地开发服务                                   |
| 路由       | React Router                | 仅管理“工作台”和“任务记录”两个页面                                       |
| 服务端状态 | TanStack Query              | 获取批次/任务状态、轮询处理进度、触发重试和下载                          |
| 样式       | CSS Modules + CSS Variables | 实现效果图中的浅色工作台、左右分栏、状态标签与响应式布局；不引入模板系统 |
| 表单与校验 | React Hook Form + Zod       | Excel 导入、尾注、标题、标签和口令的编辑校验                             |
| 图标       | Lucide React                | 导入、下载、预览、刷新、筛选等基础图标                                   |
| 视频预览   | 原生 HTML `<video>`         | 播放服务端输出的 MP4，不额外引入播放器框架                               |

前端不需要状态管理库、图表库、权限组件库或 UI 模板市场。页面数据以 API 返回值为准，局部编辑状态使用 React 表单状态即可。

### 7.2 桌面端与跨平台交付

| 分类         | 技术                                                     | 用途                                                                                  |
| ------------ | -------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| 桌面壳       | Electron                                                 | 在 Windows 与 macOS 上运行同一套 React 界面、任务服务和本地文件能力                   |
| 打包工具     | 自定义 Electron 打包脚本                                 | 复制 Electron 官方预构建二进制与应用资源；避免当前供应链策略禁止的 Forge Git 型子依赖 |
| 主进程       | Electron Main Process                                    | 创建窗口、管理应用数据目录、启动本地任务服务、调用系统文件选择/保存对话框             |
| 预加载桥     | Electron Preload + `contextBridge`                       | 只向页面暴露已白名单化的“导入、下载、选择目录、读取任务状态”能力                      |
| 进程边界     | `contextIsolation: true`、`nodeIntegration: false`       | 防止页面直接获得 Node、文件系统或任意 IPC 调用能力                                    |
| 本地服务通信 | 生产环境优先 IPC；Fastify 保留为开发模式和任务服务适配层 | 不在桌面版页面中暴露任意本机 HTTP 管理接口                                            |
| 数据目录     | Electron `app.getPath("userData")`                       | 跨平台保存 SQLite、日志、缓存与应用配置，避免依赖工作目录权限                         |

**交付目标**：macOS 交付签名/公证后的 App 或 DMG；Windows 交付签名后的应用目录或安装包。首版打包脚本先产出两端可运行的应用目录，安装器可在各自原生构建机补充。未准备证书时只能生成内部测试包；Windows SmartScreen 与 macOS Gatekeeper 可能提示风险，因此正式分发必须补充代码签名。

### 7.3 后端技术栈

| 分类       | 技术                                            | 用途                                                                       |
| ---------- | ----------------------------------------------- | -------------------------------------------------------------------------- |
| 运行时     | Node.js >= 22.13 + TypeScript                   | 统一内容读取、AI 调用、图片渲染和视频合成运行时                            |
| HTTP 服务  | Fastify                                         | 提供导入、任务查询、编辑、重试和下载 API                                   |
| 参数校验   | Zod                                             | 校验 API 请求、Excel 标准化结果和 AI 返回 JSON                             |
| 数据库     | SQLite + `better-sqlite3` + SQL 迁移文件        | 保存批次、任务、步骤、错误和输出路径；首版不使用 ORM                       |
| 任务执行   | 数据库任务表 + 单进程 Worker                    | 按状态领取任务、记录步骤、支持从失败步骤重试；不使用 Redis 或 MQ           |
| Excel 处理 | ExcelJS                                         | 读取输入表头和数据行，导出 `result.xlsx`                                   |
| 网页读取   | Playwright Chromium                             | 读取公开可访问的知乎回答与专栏页面；根据页面类型使用不同解析器             |
| AI 接入    | OpenAI 兼容 SDK + JSON Schema Structured Output | 通过一个模型适配器调用选定的大模型，并强制返回卡片、标题、标签等结构化结果 |
| 卡片渲染   | 固定 SVG 模板 + Sharp 栅格化                    | 将固定 3:4 卡片输出为 1080×1440 PNG；不依赖浏览器截图，适合离线桌面打包    |
| 视频处理   | FFmpeg                                          | 以“每张正文图片 1 秒、尾页 2 秒”规则合成 H.264 MP4，并预留背景音乐音轨     |
| 压缩与下载 | `archiver`                                      | 将批次图片与视频打成 ZIP                                                   |
| 日志       | Pino（Fastify 内置集成）                        | 为读取、AI、FFmpeg、下载失败记录可定位的任务上下文                         |

### 7.4 工程化、测试与打包

| 分类      | 技术                                      | 用途                                                                                              |
| --------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 包管理    | pnpm                                      | 固定前后端依赖与脚本                                                                              |
| 测试      | Vitest、Playwright Test                   | 单元测试任务状态/解析规则；端到端验证导入、预览和下载                                             |
| 代码质量  | ESLint、Prettier、TypeScript strict       | 保持前后端类型和格式一致                                                                          |
| 桌面打包  | Electron 官方预构建二进制 + Node 打包脚本 | 将已构建的 React 与主进程代码复制到目标平台 Electron `resources/app`；构建机按目标系统拆分        |
| 原生模块  | `@electron/rebuild`                       | 在每个目标系统按对应 Electron ABI 重建 `better-sqlite3` 与 `sharp`，再复制到可携带的 API 依赖目录 |
| 跨平台 CI | macOS Runner + Windows Runner             | 在目标操作系统上测试安装、启动、文件路径、Chromium 与 FFmpeg 侧车                                 |
| 文件目录  | Electron `userData` 与用户选择的导出目录  | 保存 SQLite、输入文件、日志与成品，不接对象存储                                                   |

首版不使用 Next.js、Remotion、Redis、BullMQ、PostgreSQL、微服务、云渲染或 Kubernetes。当前视频是静态卡片的顺序拼接，FFmpeg 已足够；复杂动画、多人协作或高并发任务出现后再评估增加相应组件。

### 7.5 模块边界

```text
Electron Main Process
  ├── 受限 Preload / IPC
  │    └── React UI
  ├── 本地任务服务
  │    ├── Excel 导入器
  │    ├── 任务编排器
  │    ├── 知乎内容读取器
  │    ├── AI 摘要器
  │    ├── 卡片渲染器
  │    ├── 视频合成器
  │    └── 导出器
  └── SQLite / 本地文件存储
```

任务必须可断点恢复。每一步将输入、输出和错误写入数据库；重试只从失败步骤继续，不重复读取或重复调用 AI。

## 8. 核心数据模型

### 8.1 Batch（批次）

| 字段                    | 说明                                                      |
| ----------------------- | --------------------------------------------------------- |
| id                      | 批次 ID                                                   |
| source_file_name        | 导入文件名                                                |
| created_at / updated_at | 创建与更新时间                                            |
| total_count             | 总任务数                                                  |
| status                  | `pending` / `processing` / `completed` / `partial_failed` |

### 8.2 ArticleTask（文章任务）

| 字段                           | 说明                             |
| ------------------------------ | -------------------------------- |
| id / batch_id                  | 标识与归属批次                   |
| source_url / source_type       | 链接及 `answer` / `article` 类型 |
| input_title / fetched_title    | 表格标题与读取标题               |
| article_keyword                | Excel 口令或经人工确认的值       |
| raw_content_path               | 原始内容快照                     |
| ai_result_json                 | AI 结构化结果                    |
| final_title / final_tags       | 可发布文案                       |
| tail_note                      | 实际渲染的尾注                   |
| status / step / progress       | 当前状态、步骤和进度             |
| failure_code / failure_message | 可读失败信息                     |
| output_dir                     | 成品目录                         |

### 8.3 任务状态

```text
pending
  → fetching
  → summarizing
  → rendering_images
  → rendering_video
  → completed

任意步骤失败 → failed
口令缺失、内容过短、AI 质检不通过 → needs_review
needs_review 经人工保存 → 从对应步骤重试
```

## 9. AI 输出契约与质量门槛

AI 必须返回可验证 JSON，不直接返回 Markdown。建议字段：

```json
{
  "sourceTitle": "原文章标题",
  "videoTitle": "不超过 24 字的发布标题",
  "tags": ["标签一", "标签二", "标签三"],
  "pages": [
    {
      "headline": "不超过 16 字",
      "body": "不超过 42 个中文字符",
      "sourceRefs": [1, 3]
    }
  ],
  "riskFlags": []
}
```

服务端校验：

- `pages` 数量在 3～15 之间。
- 标题、正文、标签均不为空且不超长。
- 每张卡片都应关联正文段落序号，便于人工追溯。
- 不允许生成没有来源依据的具体数据、结论或引语。
- 质量校验失败时进入 `needs_review`，不自动渲染。

## 10. API 草案

| 方法    | 路径                             | 作用                           |
| ------- | -------------------------------- | ------------------------------ |
| `POST`  | `/api/batches/import`            | 上传 Excel，创建批次与文章任务 |
| `GET`   | `/api/batches`                   | 获取历史批次                   |
| `GET`   | `/api/batches/:id`               | 获取批次与任务概览             |
| `POST`  | `/api/batches/:id/start`         | 启动待处理任务                 |
| `GET`   | `/api/tasks/:id`                 | 获取任务详情、预览数据与错误   |
| `PATCH` | `/api/tasks/:id`                 | 编辑标题、标签、口令、尾注     |
| `POST`  | `/api/tasks/:id/retry`           | 从失败/人工确认步骤重试        |
| `GET`   | `/api/tasks/:id/download/video`  | 下载 MP4                       |
| `GET`   | `/api/tasks/:id/download/images` | 下载图片 ZIP                   |
| `GET`   | `/api/batches/:id/download`      | 下载批次 ZIP 与结果 Excel      |

## 11. 错误处理与合规边界

| 场景                         | 系统行为                                        |
| ---------------------------- | ----------------------------------------------- |
| URL 非知乎或格式不支持       | 导入时拒绝该行并给出行号                        |
| 页面不存在/链接失效          | `failed`，记录 `SOURCE_NOT_FOUND`               |
| 页面要求登录、付费或不可访问 | `needs_review`，记录 `SOURCE_ACCESS_RESTRICTED` |
| 读取正文为空                 | `failed`，记录 `CONTENT_EMPTY`                  |
| AI 返回不合格内容            | `needs_review`，保留原始 AI 输出                |
| 卡片或视频渲染失败           | `failed`，保留中间图片和 FFmpeg 日志            |

系统只能处理用户有权访问且允许使用的内容；不保存浏览器 Cookie、账号密码或其他凭证到项目数据库。若未来需要读取登录后页面，必须采用用户明确授权的本地浏览器会话，并明确告知其访问范围。

## 12. 验收标准

1. 上传测试 Excel 后，能创建与有效链接行数一致的文章任务。
2. 知乎回答和知乎专栏链接均能被识别为不同来源类型。
3. 成功任务至少生成封面、正文图片、尾页图片、MP4 和结构化元数据。
4. 所有正文卡片时长为 1 秒；尾页默认 2 秒。
5. 输出画面为 3:4，图片和视频可独立下载。
6. 视频尾页展示当前任务保存后的口令和尾注文案。
7. 任一任务失败不阻塞同批次其他任务；失败原因可见且可以重试。
8. 口令为空、内容过短或 AI 质量不合格时，任务不会静默产出成品，而是进入“需人工确认”。
9. 页面中不存在登录、用户管理、设置、模板中心、套餐、用量或算力相关入口和文案。

## 13. 迭代顺序

### 第一阶段：可跑通

Electron 桌面壳、Excel 导入、任务表、公开内容读取、结构化 AI 摘要、固定卡片渲染、FFmpeg 视频输出、下载和失败重试。

### 第二阶段：可运营

批次记录、人工编辑、结果 Excel、批量 ZIP、背景音乐开关与固定音乐库。

### 第三阶段：按真实使用反馈决定

多视觉模板、长文自动拆分上下集、发布平台对接、协作账号体系、自动更新。第三阶段不属于当前开发范围。
